#include <stdio.h>
#include <stdlib.h>
#include <SDL2/SDL.h>

int main () {
    printf("Hello world\n");

    SDL_Window * SDL_CreateWindow(const char *title,
                                  int x, int y, int w,
                                  int h, Uint32 flags);
}
